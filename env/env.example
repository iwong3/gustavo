# Environment variables for local development
# Copy this file to .env.local and customize for your setup

# =============================================================================
# LOCAL DEVELOPMENT CONFIGURATION
# =============================================================================

# Environment
NODE_ENV=development

# Database configuration (connects to Docker PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gustavo_dev
DB_USER=gus
DB_PASSWORD=yellow_shirt_dev

# React app configuration (points to local Express server)
REACT_APP_API_URL=http://localhost:3001/api

# Express server port
PORT=3001

# =============================================================================
# PRODUCTION CONFIGURATION (for Vercel environment variables)
# =============================================================================
# Set these in your Vercel dashboard under Settings > Environment Variables

# NODE_ENV=production
# DB_HOST=your-production-db-host.com
# DB_PORT=5432
# DB_NAME=your_production_db_name
# DB_USER=your_production_db_user
# DB_PASSWORD=your_production_db_password

# Note: REACT_APP_API_URL is not needed in production (uses relative paths) 