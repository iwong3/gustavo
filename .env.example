# Copy to .env.local and fill in values
# Production vars are set in the Vercel dashboard

# Database (local Docker)
DATABASE_URL=postgresql://gus:yellow_shirt_dev@localhost:5432/gustavo_dev

# Auth.js — generate secret: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
